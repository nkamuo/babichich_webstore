{% set id = form.vars.id  %}

<label
	class="filter-label">
	{#                                                 
												<div class="form-check form-switch my-4">
													<input class="form-check-input" type="checkbox" role="switch" id="{{id}}" name="{{ form.vars.full_name }}" value="{{ form.vars.value}}" {% if form.vars.data %} checked {% endif %}>
													<label class="form-check-label" for="{{ id }}">{{ form.vars.label}}</label>
												</div>
	                                            #}

	<input type="checkbox" role="switch" id="{{id}}" name="{{ form.vars.full_name }}" value="{{ form.vars.value}}" {% if form.vars.data %} checked {% endif %}/>
	<span class="filter-checkbox rounded me-2"></span>
	<span class="filter-text">{{form.vars.label }}</span>
</label>
